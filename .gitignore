# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Secrets — never commit
.env
.env.*
!.env.example

# Generated data — reproducible from the two commands in CLAUDE.md
data/*.csv
data/*.jsonl
data/*_truth.json

# NOT ignored, deliberately:
#   cache/llm/*.json  — committed so a clone with no API key reproduces the run (SPEC §4.3)
#   models/*          — committed so the clone test does not require a training run

# OS
Thumbs.db
.DS_Store

# Personal working docs — kept locally, never published
CONTEXT_1.md
New Text Document.txt
HANDOFF.md
